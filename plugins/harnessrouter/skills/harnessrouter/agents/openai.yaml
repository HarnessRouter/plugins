interface:
  display_name: "HarnessRouter"
  short_description: "Install HarnessRouter and build agent features"
  default_prompt: "Use $harnessrouter to install or connect HarnessRouter and build an agent feature."
policy:
  allow_implicit_invocation: true
